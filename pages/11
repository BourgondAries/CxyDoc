Functions.
Functions support multiple inputs, but they MUST be named when calling the function.
<pre>
doSomething(in second):
	in = in + 1
	print(in + second)

doSomething(100, second=50)
</pre>

Only 'in' does not need to be named. This is to make code more readable.
