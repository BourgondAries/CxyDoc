Functions.
Functions also support multiple output arguments. These are returned as a tuple
<pre>
doSomething(in second : out extra):
	in = in + 1
	return in + second, second - 1

result = doSomething(100, second=50)
print(result[0])
print(result[1])
</pre>
