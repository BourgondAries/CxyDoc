Breaktime. Find the nth fibonacci number, use 64-bit integers.
<pre>
fibonacci(in):
	if in <= 0:
		return 64s(0)
	prev = [0 1]
	out = 0
	for i = 0; i < in; ++i:
		out = prev[0] + prev[1]
		prev[0] = prev[1]
		prev[1] = out
	return out

print(fibonacci(64s(10)))
</pre>

Prints
<pre>
34
</pre>
